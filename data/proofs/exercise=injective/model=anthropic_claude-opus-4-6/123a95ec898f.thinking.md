This is a classic result in mathematics: a function is injective if and only if it has a left inverse. However, there's a subtlety - this requires the domain to be non-empty for one direction (injective implies has a left inverse), and it also requires the axiom of choice.

Let me think about this more carefully in the context of Isabelle/HOL.

In Isabelle/HOL, functions are total, and types can be empty in principle, but actually in HOL all types are non-empty. So we don't need to worry about the empty domain case.

The statement: `f` is injective if and only if there exists a function `g` such that `g ∘ f = id`.


Now I'm formalizing the definitions: injectivity means that equal outputs imply equal inputs, while a left inverse is a function that undoes `f` when composed on the left. For the forward direction, if `f` is injective and `f x = f y`, then applying any left inverse `g` to both sides preserves equality, which gives us the injectivity property.

For the reverse direction, I need to construct a left inverse from injectivity. Isabelle provides `inv f`, which uses the choice operator to select a preimage for each element in the range of `f`. When `f` is injective, the lemma `inv_f_f` guarantees that `inv f (f x) = x`, making `inv f` a valid left inverse.

Now I'm writing out the formal proof in Isabelle, establishing both directions of the equivalence with the appropriate lemmas and tactics. Wait, let me check the lemma names. `inv_o_cancel` might not exist. Let me think about what's available.

In Isabelle, we have:
- `inv_f_f`: `inj f ⟹ inv f (f x) = x`
- `o_inv_o_cancel`: might exist
- `inv_o_cancel`: `inj f ⟹ inv f ∘ f = id` - I think this exists

Actually, let me check. We might need `fun_eq_iff` or `ext` to show `inv f ∘ f = id`.

From `inv_f_f`, we get `∀x. inv f (f x) = x`, which means `∀x. (inv f ∘ f) x = id x`, which by extensionality means `inv f ∘ f = id`.

Now I'm setting up the Isabelle proof structure. I'll establish the forward direction by showing that injectivity gives us a left inverse using the inverse function, then prove the reverse direction by demonstrating that a left inverse implies injectivity.

Writing the proof code... Finishing the proof for the reverse direction by showing that if a left inverse exists, then the function must be injective—the key insight is that composing both sides of an equality with the left inverse recovers the original elements. Writing the proof structure... Finishing the proof with a cleaner approach to applying the left inverse property.
