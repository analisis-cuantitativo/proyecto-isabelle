Let me formalize this statement about injective functions and left inverses.

The statement says:
f: A → B is injective iff there exists g: B → A such that g ∘ f = id_A

In Isabelle/HOL:
- Injective function: `inj_on f A` or just `inj f` for total functions
- Composition: `g ∘ f`
- Identity: `id` or `λx. x`
- The condition `g ∘ f = id` on A means: `∀x ∈ A. g(f(x)) = x`

Let me think about the proof:

(⟹) If f is injective, construct g:
- For any b ∈ B, if b is in the image of f, map it to the unique a such that f(a) = b
- If b is not in the image, map it to some arbitrary element (we need A to be non-empty for this)
- Then g(f(x)) = x for all x ∈ A

(⟸) If there exists g such that g ∘ f = id_A:
- If f(x) = f(y), then x = g(f(x)) = g(f(y)) = y
- So f is injective

In Isabelle, I'll work with functions on types. Let me write this carefully.
