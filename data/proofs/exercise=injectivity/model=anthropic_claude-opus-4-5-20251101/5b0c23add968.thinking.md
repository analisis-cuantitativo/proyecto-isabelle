I need to formalize and prove that a function f: A → B is injective if and only if there exists a function g: B → A such that g ∘ f is the identity function on A.

In Isabelle/HOL, functions are total, so I need to be careful about how to formalize this. The statement is essentially:
- f is injective ⟺ ∃g. g ∘ f = id (on A)

In Isabelle, for total functions f :: 'a ⇒ 'b, injectivity is expressed as `inj f`.

The statement "g ∘ f = id" in Isabelle would be `g ∘ f = id` or equivalently `∀x. g (f x) = x`.

So I need to prove:
`inj f ⟷ (∃g. ∀x. g (f x) = x)`

Let me think about the proof:

(⟹) If f is injective, we need to construct g such that g(f(x)) = x for all x.
- For elements in the range of f, we define g(y) to be the unique x such that f(x) = y.
- For elements not in the range of f, we can define g arbitrarily (we need A to be non-empty, or we use some trick).

Actually, in Isabelle/HOL, if A (the domain type) is empty, then f is trivially injective, but we can't have g: B → A because A is empty and B might not be. So we need to be careful.

Let me think again. In HOL, every type is non-empty, so this shouldn't be an issue.

For the forward direction, I can use `inv f` which is the inverse function in Isabelle. We have that if f is injective, then `inv f (f x) = x`.


Now I'll construct the proof. For the forward direction, if f is injective, then `inv f ∘ f = id` by the property `inv_o_cancel`. For the backward direction, if there exists g such that `g ∘ f = id`, then for any x and y where f(x) = f(y), applying g to both sides gives g(f(x)) = g(f(y)), which means x = y by the identity property, establishing injectivity. I should verify that `inv_o_cancel` actually provides what I need here. The key insight is that if `inv f ∘ f = id`, then applying both sides to any element gives us `inv f (f x) = x`, which is exactly the injectivity condition. For the reverse direction, if `f` is injective and `f x = f y`, then `x = inv f (f x) = inv f (f y) = y`, establishing the equivalence. Let me construct the full proof now.
