Let me think about this mathematical statement carefully.

A function f: A → B is surjective if for every b ∈ B, there exists an a ∈ A such that f(a) = b.

A function f: A → B has a right inverse if there exists a function g: B → A such that f(g(b)) = b for all b ∈ B.

I need to prove both directions:
1. If f is surjective, then f has a right inverse
2. If f has a right inverse, then f is surjective

Direction 2 (easier): If f has a right inverse g, then for any b ∈ B, we have f(g(b)) = b, so g(b) is a preimage of b. Thus f is surjective.

Direction 1 (harder): If f is surjective, we need to construct a right inverse. For each b ∈ B, since f is surjective, there exists at least one a ∈ A such that f(a) = b. We need to choose one such a for each b. This requires the axiom of choice!

In Isabelle, let me think about how to formalize this:
- Surjective: `surj f` or `\<forall>y. \<exists>x. f x = y`
- Right inverse: `\<exists>g. \<forall>y. f (g y) = y`

For the proof:
- One direction: assume `\<exists>g. \<forall>y. f (g y) = y`, show `\<forall>y. \<exists>x. f x = y`. This is straightforward: given y, take x = g y.


- The reverse direction requires choice: assuming `\<forall>y. \<exists>x. f x = y`, I need to construct `\<exists>g. \<forall>y. f (g y) = y`. I'll use Hilbert's choice operator with `someI_ex` to select a preimage for each element. Now I'll write the Isabelle proof, starting with the lemma statement and handling both implications.

For the forward direction, from the surjectivity assumption I can apply `someI_ex` to get `\<forall>y. f (SOME x. f x = y) = y`, then use `blast` to extract the witness. The backward direction is straightforward—given a right inverse `g`, I can show surjectivity directly since `f (g y) = y` provides the required preimage for any `y`. I should also check if Isabelle's standard library has a built-in `surj` predicate that might simplify this. f (g y) = y"
  then obtain g where "\<forall>y. f (g y) = y"
    by blast
  then show "surj f"
    by (metis surj_def)
qed

end
```

For the reverse direction, I extract the witness g from the existential and apply the surjectivity definition directly. Then I refactor the proof to be more streamlined—using `hence` and `thus` instead of explicit `have` and `show` statements, and replacing the verbose `simp add:` with more concise tactics like `unfolding` where appropriate. Given such a function g, for any y we have f(g(y)) = y, which means g(y) serves as a preimage of y under f. This directly establishes that every y has a preimage, so f is surjective.

The proof structure works in both directions: the forward direction uses the axiom of choice (via SOME) to construct a right inverse from surjectivity, while the backward direction simply applies the right inverse to show every element is in the range.
