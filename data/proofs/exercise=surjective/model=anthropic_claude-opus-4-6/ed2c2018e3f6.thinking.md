We need to show that a function f is surjective if and only if it has a right inverse, i.e., there exists a function g such that f ∘ g = id.

However, there's a subtlety: in the forward direction (surjective implies right inverse), we need the axiom of choice. In Isabelle/HOL, choice is available via the `SOME` operator.

Also, we need to be careful about the empty domain case. If the domain is empty and the codomain is non-empty, then no function from the codomain to the domain exists. But in Isabelle/HOL, all types are nonempty, so this isn't an issue.


Now I'm formalizing the definitions: f is surjective when every element in the codomain has a preimage, and f has a right inverse when there's a function g that recovers the original element after applying f. For the forward direction, I'll use the SOME operator to construct g by selecting a witness for each y, then verify that f (g y) = y holds by the properties of SOME.

Writing Isabelle proof... Finishing the proof with the surjectivity introduction rule... Finishing the proof structure...

I need to complete the existential claim from the forward direction, then handle the backward direction using metis to verify that a right inverse implies surjectivity. The key insight is that `someI_ex` lets me extract a witness from the surjectivity assumption to construct the right inverse, and then the composition property directly gives me surjectivity back. Refining the Isabelle proof approach...

I'm working through the second direction of the equivalence now—if there exists a right inverse g where f composed with g equals the identity, then f must be surjective. I extract that g and use the composition equality to show that for any y, applying f to g(y) gives back y, which directly establishes surjectivity. Finishing the proof structure...
