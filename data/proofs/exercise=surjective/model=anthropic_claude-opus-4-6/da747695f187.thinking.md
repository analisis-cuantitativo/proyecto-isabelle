We need to formalize and prove that a function is surjective if and only if it has a right inverse.

A function f is surjective means: for all y, there exists x such that f(x) = y.
A right inverse means: there exists a function g such that f(g(y)) = y for all y.

The forward direction (surjective implies right inverse) requires the axiom of choice. In Isabelle/HOL, choice is available via the `SOME` operator.

The backward direction is straightforward.

Let me think about this carefully.

Forward direction: If f is surjective, then for each y, there exists x with f x = y.

For the forward direction, I can construct a right inverse by using SOME to pick a witness for each y. Then I'll verify that f applied to this choice function recovers y.

For the backward direction, if such a g exists with f (g y) = y for all y, then f is surjective since we can always take x = g y.

Writing the Isabelle proof... Finishing the proof by showing that if such a function g exists, then f must be surjective using the appropriate lemma, though I need to verify the exact form of `surjI` to make sure I'm applying it correctly. Writing the Isabelle proof... I'm realizing I need to check the exact form of `surjI` in Isabelle/HOL — it might require showing that every element is in the range of the function rather than the equality I was trying to prove. Let me reconsider the approach and use the standard definition of surjectivity instead. Writing the Isabelle proof... I'm checking the definition of `surjI` and realizing I should just use `surj_def` directly to handle the backward direction more cleanly, since surjectivity is defined as the range being the entire codomain or equivalently that every element has a preimage.
